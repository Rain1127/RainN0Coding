# 云端验收记录

2026-09-08，在原腾讯云服务器完成。发布目录 `20260908-5a9aa750-cloud2`，源码基于主分支 `5a9aa750` 的独立部署分支，应用修复提交为 `db9ce10c`。

## 已通过

| 项目 | 结果 |
| --- | --- |
| 数据保护 | 部署前完成 MySQL、Redis、五个 Qdrant 集合和原配置备份，SHA256 全部通过；原三个容器持续运行，没有重建卷或轮换凭据 |
| 构建与测试 | 前端构建及 266 项测试；Java 打包及 68 项相关测试；Python 3 项部署回归测试通过 |
| 发布产物 | 云端 JAR SHA256 与本地一致：`128e8dad15083c3dbf79fadf9c85614ca0de64b4b26e152771010da9b8122712` |
| 平台与 API | 公网登录页面加载；专用验收账号注册、登录、创建应用通过 |
| 真实生成 | 专用应用 `454494569120382976` 完成代码生成，再经增量修改、审查、构建，SSE 返回 success；增量流程 176.1 秒 |
| 发布与交互 | `/api/static/200147/` 返回 200，JS/CSS 正常加载；浏览器验证添加、完成、删除、刷新保留完成状态 |
| 截图 | ChromeDriver 本地缓存生效；截图生成、压缩及上传 COS 成功 |
| 旧应用文件 | 从本机迁移五个旧应用代码目录和一个已发布页面目录，共 146 个文件，逐文件 SHA256 校验全部通过；旧页面 `/api/static/048650/` 已通过浏览器加载验证 |
| RAG | 512 维本地 BGE 嵌入、Qdrant 向量查询及 SQLite 关键词查询成功 |
| 指标 | Prometheus 的 java-gateway、python-agent 两个 target 均为 up |
| Grafana | Prometheus/Tempo 数据源健康，AI Workflow 仪表盘已预置 |
| 追踪 | trace `2a9ce1f9dcba4d6b90823486bb4dd7c4` 包含 Java 与 Python 两个服务、32 个 span |
| 重启保持 | 用户电脑重启后云端连续运行约 20 小时；切换修正版并重启 Java/Python 后，健康检查、已有账号和应用发布、监控历史 trace 均正常 |

## 修复与验证边界

- Python 输出目录改为可配置，与 Java 共用持久目录。
- 实际浏览器发现生成的 Vite 默认根资源路径导致子目录发布白屏。Java/Python 前端构建补充相对 base，先验证回归测试失败，再修复并在云端重新发布、浏览器操作验证。
- 第一次生成验收的客户端在 180 秒超时，不能将其记录为完整成功。服务器当时已经保存代码；后续继续已有应用完成端到端验收，并将脚本读超时增至 900 秒。
- 验收后内存快照：总 3655 MiB，可用 1865 MiB，swap 使用 427 MiB；磁盘尚余 41 GB。Java/Python 的自动重启次数均为 0。此数据不是峰值或三任务压力测试。
- 并发上限设置为 3，但未进行三路同时完整生成的压力测试。两核四 GB 适用于本次小规模演示，不能据此承诺三路复杂项目构建性能。
- 当前无域名，入口为 HTTP，尚未配置 HTTPS。监控仅允许 SSH 隧道访问。
- Node/Vue 的生成、构建、预览已实测；Go/Rust 编译器未安装，其他生成语言的完整编译运行未验收。
- 备份在同一服务器，尚未配置异地和定期备份。旧应用代码可通过 `previous` 回退，依赖与数据需独立恢复方案。
- 原工作目录中的已有未提交文件保持原样；部署改动保存在独立 worktree 和部署分支。
- 旧记录 `448315347838332928` 在本机两处生成目录中均未找到代码文件，保留数据库记录，未伪造或重新生成内容；另一旧目录 `448316610554523648` 原本只有部分规划文件。已迁移的文件保留原样，不代表这些旧生成任务均已完整成功。

运行环境：Ubuntu 22.04.5、Java 21.0.12、Python 3.12.14、Node 22.23.2、CPU Torch 2.5.1、Chrome/ChromeDriver 152.0.7977.82。监控版本及资源上限以 Compose/systemd 文件为准。
