# Merge these keys into /etc/rainn0coding/java.env; preserve its credentials.
# Enable only after SQL migration, broker verification, and queue-capable release.
APP_GENERATION_QUEUE_ENABLED=true
APP_GENERATION_QUEUE_BOOTSTRAP_SERVERS=127.0.0.1:9092
APP_GENERATION_QUEUE_TOPIC=rain-code-generation-v1
